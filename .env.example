# OpenAI key: set here OR it will be fetched from GCP Secret Manager
# (project: prj-dm-ds-v0-pdigi-dev-5gf, secret: open-ai-key, version: 3)
OPENAI_API_KEY=sk-your-openai-api-key

# Gemini uses Application Default Credentials (ADC) via Vertex AI — no API key needed
# Run: gcloud auth application-default login
GCP_PROJECT=prj-dm-ds-v0-pdigi-dev-5gf
GCP_LOCATION=us-central1

DATABASE_URL=postgres://vci:vci@localhost:5432/vci?sslmode=disable
